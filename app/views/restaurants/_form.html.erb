  <%= simple_form_for (@restaurant) do |r| %>
    <%= r.input :name %>
    <%= r.input :address %>
    <%= r.input :phone_number %>
    <%= r.input :category, selected: "chinese", collection: %w(chinese italian japanese belgian french) %>
    <%= r.button :submit %>
  <% end %>
